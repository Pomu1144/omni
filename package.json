{
  "name": "jarvis-command-center",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv backend/.venv && backend/.venv/bin/pip install -r backend/requirements.txt && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
